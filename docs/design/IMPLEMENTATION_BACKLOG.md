# Implementation Backlog

Checkboxes below reflect the actual current implementation (verified
against source, not assumed from an earlier plan) — see the file/module
named on each `[x]` line for the actual code, not just the schema.

## Epic 1 — Repository foundation
- [x] create public repository
- [x] choose license — `LICENSE` (MIT)
- [x] configure Python project — `pyproject.toml`
- [x] configure lint/type/test tooling — ruff/mypy/pytest, all in `pyproject.toml`
- [x] add CI — `.github/workflows/ci.yml` (lint/typecheck/test matrix/security jobs; verified actually running and green via the Checks API — the classic commit-status API returns nothing for Actions-based CI, which is easy to mistake for "no CI configured")
- [x] add security scanning — `bandit`/`pip-audit`, wired into the CI `security` job
- [x] add contribution guide — `CONTRIBUTING.md`

## Epic 2 — Domain model
- [x] define JSON schemas — `reporting/schema.py::export_json_schemas`, `si schema [--out DIR]`; one `*.schema.json` per canonical snapshot file plus `dashboard_data`, guarded by a test that fails if a future `Snapshot` field has no matching schema entry
- [x] implement entities — `core/entities.py`
- [x] implement relationships — `core/relationships.py`, `analysis/relationships.py`
- [x] implement evidence — `core/evidence.py`
- [x] implement snapshots — `core/snapshot.py`

## Epic 3 — Discovery
- [x] filesystem scanner — `discovery/structure.py`, backed by `discovery/paths.py::EXCLUDED_DIRS` (`vendor` — Go's `go mod vendor`/PHP Composer's own dependency-vendoring convention, a full copy of every dependency's own source tree including its own manifest — plus `.tox`/`target`, the same "dependency cache/build output" shape already covered for other ecosystems); a vendored dependency's own go.mod/package.json/etc. was previously discovered as if it were the target repository's own, misattributing its transitive dependencies. `env` (bare, no leading dot) rounds out the already-excluded `.venv`/`venv`: a real, common `python -m venv env` convention (widely used in older/tutorial-derived Django and Flask projects) whose installed third-party packages' own `setup.py`/`pyproject.toml` were previously misattributed the same way, verified live against a real virtualenv layout. `LANGUAGE_EXTENSIONS` also recognizes PHP/C/C++/C#/Kotlin/Swift — six common, unambiguous single-language extensions entirely unrepresented before, so a repository written in any of them previously contributed zero file counts for its own primary language (`.h`/`.hpp` headers deliberately excluded — shared between C and C++, unlike every other entry)
- [x] Git scanner — `discovery/git_metadata.py`; `default_branch` resolves via `git symbolic-ref refs/remotes/origin/HEAD` (the remote's own default branch, set locally at clone time), not `git rev-parse --abbrev-ref HEAD` (this working tree's currently checked-out branch) — the two only coincide when HEAD happens to be attached to the remote's default branch. Live-validated against a real clone (`octocat/Hello-World`): correct (`master`) with HEAD attached, and still correct after `git checkout --detach` — where the previous, buggy logic would have reported the literal, nonsensical branch name `"HEAD"`, since a CI checkout of a specific commit SHA (the common case for GitHub Actions and similar) leaves HEAD detached. No remote configured leaves `default_branch` unset rather than guessed, since this collector is local-only and never makes a network call (e.g. `git remote show origin`) to resolve it
- [x] GitHub adapter — `discovery/github_target.py::clone_github_repository`; `owner/repo` shorthand or a `https://github.com/...` URL is shallow-cloned read-only (`git clone --depth 1`) into a temp directory, then resolved exactly like any local path (`discovery/target.py::resolve_target`), so every existing `si` command gained GitHub-target support with no per-command changes. This clone never authenticated at all — `GITHUB_TOKEN` (the same env var `cli/main.py` already reads for GitHub research/write operations, and `execution/github_pr.py::push_branch` already uses to authenticate its own git invocation) was silently ignored here, so any `si <command> owner/private-repo` target failed with an opaque git error giving no indication a token would help, even with one correctly exported exactly as the README instructs. Fixed by applying `push_branch`'s own established pattern (a one-off `-c http.extraheader=AUTHORIZATION: basic <base64(x-access-token:token)>` scoped to this single `git clone`, never in the URL or written to any git config) when `GITHUB_TOKEN` is set. Also added `GIT_TERMINAL_PROMPT=0` (git's own documented boolean env var, "will not prompt on the terminal... when asking for HTTP authentication") so an unauthenticated/inaccessible private repo fails fast with git's own stderr instead of this non-interactive process blocking indefinitely on a credential prompt it could never answer. Live-validated against the real github.com: a normal public clone still succeeds unchanged; a real nonexistent-repo clone now fails in under a second citing "terminal prompts disabled" instead of hanging; and adding a token (even an invalid one, to avoid needing real credentials in this environment) demonstrably changes GitHub's own server response from success to an auth-related failure, proving the header is genuinely transmitted and processed, not silently dropped
- [x] Agent Skills detector — `discovery/skills.py`; the upstream spec's three optional Skill subdirectories (docs/design/docs/00-research-baseline.md; docs/design/docs/17-reference-workflows.md's "inspect scripts/references/assets") are `scripts/`, `references/`, and `assets/`, but only the first two were ever listed — the `Skill` model had no `assets` field at all, and nothing populated one, so a Skill bundling only `assets/` (images, templates, data files it reads at runtime, no `scripts/`/`references/`) silently reported empty listings for all three. Fixed by adding `Skill.assets` and listing `skill_dir / "assets"` the same way `scripts`/`references` already are. Live-validated against the real `anthropics/skills` repo's own `skill-creator` Skill (fetched live via `git clone`): its real `assets/eval_review.html` is now correctly detected
- [x] `Repository.license` — has existed on the model since the Phase 1 foundational commit (docs/design/docs/05-analysis-engine.md's "Repository" detector family explicitly lists "license" alongside "structure"/"language"/"package manager", all of which were already implemented), but a full-repo grep confirmed zero writers of it: `discovery/ci_docs.py::detect_root_documents` only ever recorded *that* a `LICENSE`/`LICENSE.md`/`LICENSE.txt` file exists, never read its content, and both `discovery/inventory.py` and `intelligence/orchestrator.py::_run_ci_docs_detection` never set the field either — completely dead on both ends (the only `.license` reads anywhere in the codebase are all `ResearchResult.license`, a separate model for external research candidates, never `Repository.license`). Fixed by adding `discovery/ci_docs.py::detect_license`, a conservative content classifier matching a detected LICENSE file's text against `_LICENSE_SIGNATURES` — nine well-known SPDX identifiers (MIT, Apache-2.0, GPL-3.0, GPL-2.0, BSD-3-Clause, BSD-2-Clause, ISC, MPL-2.0, Unlicense), each an exact, distinctive substring verified live against spdx/license-list-data's own canonical reference texts (never guessed from the filename alone, and ordered so a more specific license's text, which a less specific relative's substring is also found in, is checked first — e.g. BSD-3-Clause's own third "Neither the name of..." clause before BSD-2-Clause's shared first two clauses). Wired into both `discovery/inventory.py::discover_local_repository` and `intelligence/orchestrator.py::_run_ci_docs_detection` (keeping the two construction paths from drifting on this new field from the start), and surfaced in `si inspect`'s output alongside the other repository facts it already prints (`Git repository`, `Languages`). Live-validated against this very repository's own real `LICENSE` file: `si inspect .` now correctly prints `License: MIT`, where it previously never appeared at all
- [x] `detect_license`'s Apache-2.0/BSD-3-Clause signatures didn't match real-world LICENSE file formatting — `discovery/ci_docs.py`. The Apache-2.0 signature (`"Apache License\nVersion 2.0"`) required that exact, unwrapped adjacency, but the real, official Apache Software Foundation text (`curl https://www.apache.org/licenses/LICENSE-2.0.txt`, verified live) centers "Apache License" / "Version 2.0, January 2004" on separate lines with ~27 leading spaces each — the literal substring never appears in it, confirmed against a real, byte-identical copy (Kubernetes' own root `LICENSE`): every signature returned `False`, so a canonical Apache-2.0 repository's license was silently reported as `unknown`. Separately, the BSD-3-Clause signature hard-coded the generic SPDX placeholder ("...nor the names of its contributors") — real BSD-3-Clause files substitute the actual org name (protobuf's real `LICENSE`: "Neither the name of Google Inc. nor the names of its contributors"; NumPy's: "...the NumPy Developers nor the names of any contributors", verified live against both), so real BSD-3-Clause repositories were also silently reported as `unknown` -- and, once the whitespace fix alone is applied without this one, actively *misclassified* as the less-specific `BSD-2-Clause` instead (confirmed empirically: worse than the pre-fix `unknown`, an evidence-first violation in the wrong direction). Fixed by (1) collapsing whitespace runs to a single space in both the signature and the file text before matching (`_normalize_whitespace`), and (2) replacing the BSD-3-Clause literal with a regex (`_BSD_3_CLAUSE_RE`, `"Neither the name of .+? nor the names of (?:its|any) contributors"`) that still subsumes the original generic-placeholder text while tolerating a real org name and either "its"/"any" — `_LICENSE_SIGNATURES` now accepts either a literal substring or a compiled pattern per entry, in the same specific-before-general order as before. Live-validated against six real, independently-sourced LICENSE files: Kubernetes' Apache-2.0, protobuf's and NumPy's BSD-3-Clause (two different org-name/its-vs-any variants), Mozilla's own MPL-2.0 reference text, and Linux's and git's real GPL-2.0 files — all six now correctly classified, where Apache-2.0 and both BSD-3-Clause cases previously failed or misclassified; also end-to-end via the real `si inspect` CLI command against Kubernetes' real LICENSE file (`License: Apache-2.0`, previously `License: unknown`)
- [x] `Repository.last_commit_sha`/`last_commit_author`/`last_commit_date`/`is_dirty` — the same "field is populated with real data, no consumer ever reaches it" shape as the `Repository.license` bullet immediately above, but one step further back in the pipeline: `discovery/git_metadata.py::collect_git_metadata` already ran real `git log -1`/`git status --porcelain` subprocess calls and populated these four `GitMetadata` fields (each backed by its own `Confidence.VERIFIED` Evidence) since the module's original Phase 2 implementation, but both `discovery/inventory.py` and `intelligence/orchestrator.py::_run_git_metadata` only ever copied `remote_url`/`default_branch` off it onto the `Repository` being built — the other four were computed and then silently discarded on both construction paths. docs/design/docs/05-analysis-engine.md's "Lifecycle" detector family explicitly lists "last modification" as a required signal, which `last_commit_date` directly answers. `Repository` had no fields for any of this, so — mirroring the exact same schema-addition pattern the `license` fix immediately above just established — added `last_commit_sha`/`last_commit_author`/`last_commit_date` (`str | None`, keeping `git log`'s own `%cI` ISO 8601 string rather than parsing it, since nothing here needs to compute with it) and `is_dirty` (`bool | None`) to `Repository`, wired into both construction paths, and surfaced a `Last commit: <date>[, uncommitted changes]` line in `si inspect`'s output (only when known, mirroring `License`'s `unknown` fallback pattern but omitting the line entirely rather than printing a placeholder, since "not a git repository" is already conveyed by the existing `Git repository: no` line). Live-validated against this very repository's own real git history: `si inspect .` now correctly prints the real last-commit timestamp and author, and correctly flags the working tree as dirty when it has uncommitted changes, where none of this ever appeared before
- [x] `Repository.is_git_repository` — a variant of the same "field computed by the producer but never carried onto the entity" shape as the two bullets immediately above, compounded by "the wrong existing field used as the boolean signal" in two independent renderers. `discovery/git_metadata.py::GitMetadata.is_git_repository` was correctly computed (`git rev-parse --is-inside-work-tree`) since the module's original implementation — its own docstring even explicitly notes `default_branch` is deliberately left unset when there's no resolvable remote symref, distinct from repo-ness — but `Repository` had no field to hold it, so both `cli/main.py`'s `inspect` command and `reporting/html.py`'s `_render_overview` fell back to using `default_branch`'s truthiness as a proxy for "is this a git repo", since that was the only git-related fact actually reaching `Repository`. `default_branch` only resolves when a remote is configured *and* its `HEAD` symref has been locally set (typically via a fetch) — so a real, valid git repository with real commits but no `origin` remote (or one never fetched) produced the self-contradictory `Git repository: no` / `not a git repository` output sitting right next to a real last-commit timestamp from that same repo (confirmed live: `si inspect` against a real `git init`+commit with no remote configured). Fixed by adding `Repository.is_git_repository: bool` and wiring it into both `discovery/inventory.py` and `intelligence/orchestrator.py::_run_git_metadata` (the same two construction paths every other `GitMetadata`-derived field already goes through), then switching both `cli/main.py` and `reporting/html.py` to branch on it instead of `default_branch`, with a new `"yes (no remote branch detected)"` state for a real repo with an unresolvable default branch, keeping `"not a git repository"`/`"no"` reserved for when `is_git_repository` is actually `False`. Live-validated against three real cases: a genuine non-git directory (`Git repository: no`, unchanged), a real git repo with commits and no remote (now correctly `Git repository: yes (no remote branch detected)`, previously the incorrect `no`), and this very repository's own real git history (`Git repository: yes (no remote branch detected)`, matching its actual remote-less local clone in this environment)
- [x] `Repository.url` (the discovered git remote) — genuinely populated by both construction paths from the start (`discovery/git_metadata.py::collect_git_metadata`'s real `git remote get-url origin`, its own dedicated `Confidence.VERIFIED` Evidence; copied onto `Repository.url` by both `discovery/inventory.py` and `intelligence/orchestrator.py`), the same "field populated, never read" shape as `license`/`last_commit_*`/`is_git_repository` above, but on the one `GitMetadata`-derived fact none of those fixes happened to touch: neither `si inspect`'s output nor the static report's Overview `<dl>` (`reporting/html.py::_render_overview`) ever referenced `.url`, even though both already print every sibling fact from the same struct (`is_git_repository`/`default_branch`, `languages`) right next to where it would go. Concretely, "where does this repository actually come from" — one of the most basic facts a repository-intelligence tool should surface, and one it had already verified — was invisible in every user-facing view. Fixed by adding a `Remote: <url or 'none configured'>` line to `si inspect`, mirroring `License`'s `unknown`-fallback pattern, and a matching `<dt>Remote</dt>` row to the static report's Overview `<dl>`, right after `Git branch`. Live-validated against three real cases: a real git repo with no configured remote (`Remote: none configured`, both surfaces), a real git repo with a real configured `origin` remote (`Remote: https://example.com/octocat/demo.git`, both surfaces), and this very repository's own real `origin` remote (`si inspect .` now correctly prints `Remote: https://github.com/kajisho5/system-intelligence`, where it never appeared before)
- [x] agent detector — `discovery/agents.py::detect_agents`; detects exactly one explicit, self-declared convention (Claude Code's own `.claude/agents/*.md` subagent format, sharing `SKILL.md`'s `---`-delimited frontmatter shape via the new `discovery/frontmatter.py`) rather than attempting to guess at the many mutually-incompatible "agent" conventions across ecosystems (LangChain/CrewAI/AutoGen/OpenAI Assistants, ...), which would mean inferring formats this project has no verified contract for (ADR-002/ADR-007). `model_provider`/`tool_names` are only ever populated from the frontmatter's own `model:`/`tools:` fields, verbatim, never inferred. `Agent.permissions` had existed on the model since it was first introduced, but nothing ever populated it — `disallowedTools` (verified against code.claude.com/docs/en/sub-agents's own "Supported frontmatter fields" table: "Tools to deny, removed from inherited or specified list", and confirmed as a real, commonly-used field via GitHub code search across public `.claude/agents/*.md` files) is parsed from `discovery/frontmatter.py`'s already-captured generic fields dict exactly like `tools:`/`tool_names` already are, just never read out into the `Agent` being built. Live-validated against a real fixture (`disallowedTools: Write, Edit`): `Agent.permissions` now correctly resolves to `["Write", "Edit"]`, where it previously would have stayed permanently empty. `Skill` had no equivalent fields at all: `allowed-tools`/`disallowed-tools` are the documented Agent Skills counterparts (verified against code.claude.com/docs/en/skills's own field reference) to `Agent`'s `tools:`/`disallowedTools:`, but `discovery/skills.py` only ever read `name`/`description` back out of its already-captured frontmatter fields dict — everything else, including these two, was silently discarded. Fixed by adding `Skill.tool_names`/`Skill.permissions` and parsing `allowed-tools`/`disallowed-tools` the same way. Both this fix and the pre-existing `Agent` parsing previously split on a bare comma, which the same sub-agents docs show is unsafe: `Agent(worker, researcher)` is one tool specifier whose parenthesized argument list itself contains a comma, which `.split(",")` breaks into two tokens. Replaced with `discovery/frontmatter.py::split_tool_list`, a small paren-depth-aware tokenizer used by both `agents.py` and `skills.py` now. Live-validated against the real, vendored `playwright-cli` `SKILL.md` (`allowed-tools: Bash(playwright-cli:*) Bash(npx:*) Bash(npm:*)`, space-separated with internal colons/asterisks): correctly resolves to three distinct tool specifiers, not one run-together string. `detect_agents` also only ever scanned `.claude/agents/*.md` directly (not recursively), on a documented rationale — "Claude Code itself only ever reads project-level subagents from that fixed path" — that turned out to be factually wrong: the official docs state Claude Code "scans `.claude/agents/`... recursively, so you can organize definitions into subfolders such as `agents/review/`", confirmed as a real, commonly-used convention via GitHub code search (152 real files under `.claude/agents/review/*.md`-shaped paths across public repositories). A target repository organizing its subagents into subfolders had every one of them silently invisible to `si diagnose`/`si inspect` — no `Agent` component, no capability extracted, no unused-agent audit. Fixed by switching `agents_dir.glob("*.md")` to `agents_dir.rglob("*.md")` (mirroring `detect_skills`'s own recursive `SKILL.md` search) and correcting the docstring; `is_excluded` already applies correctly to nested paths with no further change needed. Live-validated against a real fixture nested at `.claude/agents/review/security.md`: now correctly discovered with `path` reflecting the full nested relative path. `Skill.triggers` had also existed on the model since Phase 1 (docs/design/docs/05-analysis-engine.md lists it alongside `scripts`/`references`) but nothing ever populated it — `paths` is the documented Agent Skills frontmatter field it maps to (code.claude.com/docs/en/skills: "Glob patterns that limit when this skill is activated... Accepts a comma-separated string or a YAML list" — only the comma-separated string form is parseable by this project's flat-key-value frontmatter parser, the same documented limitation every other multi-value field here already has). Parsing it with a bare comma-split would break real, standard brace-expansion glob syntax (`src/**/*.{ts,tsx}`) the same way the earlier tool-list comma-split broke `Agent(worker, researcher)`, so a new `discovery/frontmatter.py::split_glob_list` mirrors `split_tool_list`'s depth-tracking, just for `{...}` instead of `(...)`, and comma-only (never bare whitespace, since a glob pattern could itself contain a space). Live-validated against a real fixture (`paths: src/components/**/*.{tsx,jsx}`): `Skill.triggers` now correctly resolves to `['src/components/**/*.{tsx,jsx}']` as one pattern, where it previously would have stayed permanently empty
- [x] package detector — `discovery/structure.py`'s `PACKAGE_MANIFESTS` (includes `Pipfile` for Pipenv-only Python projects, `build.gradle.kts` for Gradle's Kotlin DSL, `requirements.txt` — the *only* manifest many non-packaged Python projects have at all, with no pyproject.toml/setup.py/Pipfile in sight — and `composer.json` for `packagist`/PHP, rounding out the PHP support `LANGUAGE_EXTENSIONS` already had with no matching manifest entry, verified live against the real `composer/composer` project's own `composer.json` — alongside each ecosystem's other already-listed manifest filenames)
- [x] CI detector — `discovery/ci_docs.py::detect_ci_jobs`
- [x] docs/ADR detector — `discovery/adr.py::detect_adrs`; matches the `ADR-<number>` filename convention (verified against `ai-video-production-os`'s `docs/adr/ADR-NNN-slug.md`), parses `Status:` when present, never fabricates `decided_at`; wired into `Snapshot.adrs` and the Dashboard's new "Architecture Decisions" section

## Epic 4 — Analysis
- [x] dependency graph — `analysis/dependencies.py` (pyproject.toml/requirements.txt, package.json, Cargo.toml, go.mod, and pom.xml manifests parsed; build.gradle/Gemfile still detected as ecosystem evidence only, not yet parsed — Gradle's Groovy DSL and Gemfile's executable-Ruby format aren't declarative data formats a direct port of the existing pattern could safely parse). `_extract_pom_dependencies` is scoped to exactly `/project/dependencies/dependency` (never `dependencyManagement`'s, a `<profile>`'s, or a `<plugin>`'s own nested `<dependencies>`, verified against real pom.xml files where a build plugin's own literal-version dependency sits right next to the project's), and only records a dependency whose `<version>` is a literal value — one with no `<version>` at all (parent/dependencyManagement-resolved) or a `${property}`-templated one (resolved via `<properties>` or a parent POM, possibly in another file) is skipped, never guessed. Parses via `defusedxml` (added as a real, direct dependency — not the stdlib `xml.etree.ElementTree`, which bandit correctly flags as XXE/entity-expansion-unsafe for the untrusted XML a GitHub-target scan can encounter) + `analysis/relationships.py` (DEPENDS_ON, now Component-level, not just Repository-level). `_extract_requirements_txt_dependencies` treats each remaining line as one PEP 508 requirement string (reusing `_parse_pep508`, the same parser pyproject.toml's `[project.dependencies]` already uses), skipping pip's requirements-file-only syntax it has no registry-resolvable name/version pair for: comments, option flags (`-r`/`-e`/`-c`/`--index-url`/a `--hash=...` continuation line, all starting with `-`), and direct URL/VCS references (identified by `://`). `_extract_poetry_dependencies` also parses Poetry's own pre-PEP-621 `[tool.poetry.dependencies]` table (still the form most existing Poetry projects use — a Poetry-native pyproject.toml previously reported zero dependencies at all, despite being registered as a `pypi` manifest). A bare version there (`requests = "2.31.0"`) means an *exact* pin under Poetry's own convention (confirmed against Poetry's official docs' "Exact requirements" section, not guessed — the opposite of Cargo's bare-is-caret convention), normalized to PEP 508's own `==`-prefixed form so `_EXACT_PIN_RE` applies unchanged with no Poetry-specific carve-out; a wildcard (`1.*`) is still correctly rejected downstream since `*` isn't in `_EXACT_PIN_RE`'s allowed character class even after normalization. Live-validated end-to-end (`si check-updates`) against a real Poetry-native pyproject.toml: the bare-pinned `requests` correctly resolves real PyPI freshness data, the caret-pinned `click` correctly stays "unknown"
- [x] lockfile-based version resolution (Cargo only so far) — `analysis/dependencies.py::_extract_cargo_lock_resolved_versions`; `Dependency.resolved_version` and its `Confidence.VERIFIED` consumption in `analysis/update_intelligence.py::build_current_state` already existed, but nothing ever produced it for any ecosystem. A Cargo dependency's sibling `Cargo.lock` (single-crate projects only — a workspace's lockfile lives only at the workspace root, never guessed at from a member crate's own directory) now resolves it for an unambiguous package name, even when the declared constraint is a range (`anyhow = "1.0"` means "whichever 1.0.x is actually locked", not just "some 1.0.x exists"). A name with more than one `[[package]]` entry (a real, common case for a transitive dependency with conflicting major versions — verified against a real `Cargo.lock` where `syn` appears twice) is left unresolved rather than guessed at. Live-validated end-to-end (`si check-updates`) against the real `ripgrep` project's `Cargo.toml`/`Cargo.lock`: every direct dependency's range-constrained current version now resolves to its real locked version (e.g. `anyhow = "1.0.75"` → resolved `1.0.104`, verdict `no_update_available (verified)` since that's also the latest). The npm/pip equivalents (`package-lock.json`, `poetry.lock`) are not implemented yet
- [x] architecture heuristics — `analysis/architecture.py` (AST-based circular-import detection); `detect_circular_dependencies` was the last remaining Finding-producing analyzer that never set `affected_entity_ids` (same class of gap as `analysis/gaps.py` above) — it took only a bare `Path`, with no `Repository`/entity-id parameter at all, even though both call sites (`analysis/engine.py`, `intelligence/orchestrator.py`) already had `repository`/`ctx.repository` in scope for their neighboring `audit_documentation`/`audit_ci_and_tests` calls. A `circular_dependency` Finding's `members` are bare Python module-name strings, not Component ids, so the Repository is the only entity this can truthfully attribute the finding to — same reasoning as `gaps.py`. Fixed by adding a `repository: Repository` parameter and setting `affected_entity_ids=[repository.id]`. Live-validated (`analyze_local_repository`) against a real two-module import cycle: the `circular_dependency` Finding's `affected_entity_ids` now correctly resolves to the real Repository component's own id, where it was previously always `[]`, so it can now appear in the Dashboard's per-component "Findings affecting this component" panel like every other Finding category
- [x] capability extraction — `analysis/capabilities.py`; the module's own docstring said "Agents ... will extend this once their detectors exist" — but `discovery/agents.py::detect_agents` has existed since Epic 3's agent detector (well before this fix), and `discovery/inventory.py` already wires its results into `Snapshot.components`; `extract_capabilities` simply never accepted them, so no Agent ever became a `Capability`. Concretely, this meant `analysis/gaps.py::audit_capability_gaps` could report a HIGH-severity `capability_gap` false positive for any `.si/requirements.json`-declared capability actually satisfied by an Agent rather than a Skill — a real evidence-first-violating false positive, since the "missing" capability was verifiably present. Fixed by adding `Agent.is_standard_format` (mirroring `Skill.is_standard_format`; `discovery/agents.py` had already computed this signal locally but discarded it, never storing it on the entity) and generalizing `extract_capabilities` to `list[Skill | Agent]`, wired into both `analyze_local_repository` and `intelligence/orchestrator.py`'s `capability_extraction` runner (now also requiring `agent_detection`). Live-validated end-to-end (`si diagnose`): a real `.claude/agents/code-reviewer.md` Agent correctly satisfies a `.si/requirements.json` declaration for `"code-reviewer"` with zero `capability_gap` findings, where it previously would have been falsely flagged
- [x] unused candidate analysis — `analysis/unused.py`; `audit_unused_skills`/`classify_skill_usage` were typed to `Skill` only, but neither ever reads a Skill-specific field (only `name`/`path`, both inherited from `Component`) — written before `discovery/agents.py`'s detector existed and never updated afterward, the same pattern as the `extract_capabilities` fix above. An unreferenced `.claude/agents/*.md` Agent silently got zero `unused_candidate` finding, while an identically-unreferenced Skill correctly did. Fixed by widening both to `Skill | Agent` and passing `[*skills, *agents]` at every call site (`analyze_local_repository`, the orchestrator's `unused_skill_detection` runner, now also requiring `agent_detection`). Live-validated (`si diagnose`) against a real fixture: a genuinely-unreferenced `.claude/agents/orphan-agent.md` now correctly produces its own `unused_candidate` finding. `_find_references`'s own name match was also a plain `component.name in text` substring containment check, not word-bounded — unlike every other name-match in this codebase (`analysis/capabilities.py`'s exact dict-key matches; `proposals/engine.py`'s `\b`-bounded, `re.escape`d manifest-patcher regexes). A component named e.g. `read` was falsely classified `UNKNOWN` ("referenced") by matching inside unrelated prose like "alr**read**y" — the bug is one-directional (only ever more lenient, never a false unused flag), so it silently suppressed exactly the `unused_candidate` findings this detector exists to surface for any short/common component name (`read`, `run`, `test`, `docs`, ...). Fixed by matching a `\b`-bounded, `re.escape`d pattern instead of a bare substring. Live-validated against a real fixture (a Skill named `read`, a README containing only "This project **already** has extensive documentation"): now correctly classified `UNREFERENCED`, where it was previously falsely `UNKNOWN`; a genuine exact-word reference ("See the 'read' skill for details") is still correctly detected
- [x] duplication detection — `analysis/capabilities.py::detect_duplicate_capabilities`
- [x] gap detection — `analysis/gaps.py::audit_capability_gaps`; opt-in via `.si/requirements.json` (the same `.si/` convention `si research`'s cache directory already uses) declaring required capabilities by name. A repository with no such file contributes zero `capability_gap` findings, never an inferred one; a malformed one surfaces as its own `invalid_requirements_file` finding rather than crashing `si diagnose`. Wired into `analyze_local_repository`, so it runs automatically wherever `si diagnose`/`si report`/`si dashboard` already do. Unlike every sibling analyzer (`audit_documentation`, `audit_ci_and_tests`, `audit_unused_skills`), neither `capability_gap` nor `invalid_requirements_file` Findings ever set `affected_entity_ids` — the module had no `Repository` parameter to attach at all, even though both callers (`analysis/engine.py`, `intelligence/orchestrator.py`) already had one in scope for the sibling calls right next to it. Concretely, this meant these two Finding categories could never appear in the Dashboard's per-component "Findings affecting this component" panel (`reporting/dashboard_html.py`'s `affected_entity_ids`-filtered views) or its Findings list's "Affects: ..." chip, unlike every other category. Fixed by adding a `repository: Repository` parameter to `detect_capability_gaps`/`audit_capability_gaps`/`_invalid_requirements_finding`, mirroring `audit_documentation`'s own parameter, and setting `affected_entity_ids=[repository.id]` on both Finding constructions. Live-validated (`si diagnose`) against a real fixture declaring an undeclared `pdf export` capability: the resulting `capability_gap` Finding's `affected_entity_ids` now correctly resolves to the real Repository component's own id, where it was previously always `[]`
- [x] capability registry / dynamic selection (docs/design/docs/10-plugin-skill-system.md) — `intelligence/registry.py::CAPABILITIES`; three real, already-shipped detectors (`agent_detection`, `adr_detection`, `capability_gap_detection`) had no entry at all, even though `discover_local_repository`/`analyze_local_repository` (the actual `si inspect`/`si diagnose` implementations) always run them — confirmed via `git blame` that `registry.py`/`intents.py` predate `detect_agents`/`audit_capability_gaps` and were never updated afterward. This made `si plan diagnose`'s preview silently omit three things `si diagnose` actually does, and meant `run_capabilities` (the intent-driven selective-execution engine) could never reach them no matter what was requested — a "half-implemented feature" gap in the registry/orchestrator abstraction itself, not in any detector. Fixed by registering all three (`capability_gap_detection` requires `capability_extraction`, matching `gaps.py`'s own dependency on already-extracted `Capability` records) and adding matching runners to `orchestrator._RUNNERS`; `inspect`'s and `diagnose`/`improve`'s capability sets now include them, matching what those commands actually execute. Live-validated: `si plan diagnose` now lists all 14 capabilities (previously 11), and `run_capabilities` against a real fixture with a `.claude/agents/*.md` file and a `docs/adr/ADR-001-*.md` file correctly produces an `Agent` Component and populates `Snapshot.adrs`. Beyond registration, the two orchestration engines had also independently drifted on *how* they attribute dependencies: `analyze_local_repository` (backing `si diagnose`) already attributed each manifest's dependencies to whichever Component's own directory it lives in (a Skill's own `package.json` becomes that Skill's dependencies), but `run_capabilities`'s `_run_dependency_extraction` called the flat `extract_dependencies` and unconditionally dumped every manifest's dependencies onto the Repository regardless of which Component actually owned the directory — so the same "diagnose" intent produced two contradictory facts about which component depends on what (and a correspondingly wrong `DEPENDS_ON` relationship source) depending on which engine ran it. Fixed by extracting the shared attribution logic (`analysis/dependencies.py::attach_dependencies_by_component`, moved out of `analysis/engine.py`'s own private helper) and having `_run_dependency_extraction` call `extract_dependencies_by_manifest` + this shared function the same way `analyze_local_repository` already does, so the two engines cannot independently drift on this again. Live-validated against a real fixture (`skills/demo/SKILL.md` + `skills/demo/package.json` declaring `left-pad`): both `analyze_local_repository` and `run_capabilities(..., resolve_intent("diagnose"))` now correctly attribute `left-pad` to the Skill (not the Repository) and produce a matching `DEPENDS_ON` edge — previously the two engines disagreed. A third instance of the same drift: `analyze_local_repository` always calls `analysis/capabilities.py::attach_consumers` (populating `Capability.consumer_ids` from a Component's own `Dependency.name` matching a Capability's name) before `build_relationships`, but `run_capabilities` never called it at all — `_run_relationship_graph_construction` built relationships directly from `ctx.capabilities`, whose `consumer_ids` could only ever be `[]`, so `build_relationships`'s own `USES`-edge logic (which reads `consumer_ids`) was permanently dead code for this engine, contradicting `relationship_graph_construction`'s own registry description ("Materialize DEPENDS_ON/PROVIDES/USES/DUPLICATES edges"). Fixed by calling `attach_consumers` inside `_run_relationship_graph_construction` itself — the one place its own registry `requires={"dependency_extraction", "capability_extraction"}` already guarantees both prerequisites ran first, regardless of which capabilities were directly requested. Live-validated against a real fixture (a Skill named `demo` plus a root `package.json` declaring a dependency literally named `demo`): both engines now correctly produce one `USES` edge from the Repository to the `demo` capability, where `run_capabilities` previously always produced zero
- [x] documentation audit — `analysis/documentation.py`, fed by `discovery/ci_docs.py::detect_root_documents`'s case-insensitive match against the actual on-disk filename (`readme.md`/`License`/bare `README` all count, not just the canonical `README.md`/`LICENSE` spelling) — a case-sensitive filesystem (Linux) previously reported a real, differently-cased README/LICENSE as a `documentation_gap` finding, a plain false positive GitHub's own README/LICENSE detection doesn't produce either. `_ROOT_DOCUMENTS` also recognizes `CODE_OF_CONDUCT.md` — one of GitHub's own "community health file" conventions, the same family `CONTRIBUTING.md`/`SECURITY.md` are already drawn from — recorded when present the same as `SECURITY.md`, never turned into a mandatory gap finding when absent (`_EXPECTED_DOCUMENT_TYPES` deliberately excludes both)
- [x] test/CI audit — `analysis/ci_quality.py`; `_has_test_files` previously only ever looked under a `tests/` directory for Python/JS/TS filename patterns, so a real, fully-tested Go project (whose own convention colocates `<name>_test.go` directly next to the source file it tests, never under `tests/` at all) was unconditionally flagged with a HIGH-severity `test_gap` finding regardless of actual test coverage — fixed by also scanning the whole tree (via `discovery/paths.py::iter_files`, so vendor/build directories stay excluded) for `*_test.go`. A bare `*.rs` under `tests/` was also added to the directory-based patterns: every file there is compiled by Cargo as its own integration-test crate, unambiguously a test file, unlike the existing patterns which were Python/JS/TS-only. `_has_maven_layout_test_files` also recognizes Maven's own "Standard Directory Layout" (verified against maven.apache.org's own docs) — test sources at `src/test/java`, never under `tests/` — matching Gradle's Java plugin default too; Java is already a first-class ecosystem here (`pom.xml`/`build.gradle(.kts)` detection, a real `pom.xml` dependency extractor). `_TEST_FILE_PATTERNS` also recognizes `*Test.php`, PHPUnit's own default naming convention (verified against PHPUnit's own manual) — PHP is already a first-class ecosystem here (`.php`/`composer.json` detection). `_has_kotlin_layout_test_files` mirrors `_has_maven_layout_test_files` exactly, just for `src/test/kotlin` — the Kotlin Gradle plugin's own parallel source-set convention alongside `src/main/kotlin` (Kotlin is already a first-class ecosystem here — `discovery/structure.py` maps `.kt` to "Kotlin" and documents `build.gradle.kts` as the default manifest for new Kotlin/Android projects). Unlike Maven's/PHPUnit's own official docs, kotlinlang.org's own convention page renders no fetchable static content (JS-only shell) and Gradle's own docs stop short of naming `src/test/kotlin` explicitly — so this was verified instead against 290k+ real `src/test/kotlin/**/*.kt` files across public GitHub repositories (GitHub code search), consistent with this project's existing practice of verifying against real repository layouts, not just written docs, when a first-party doc source doesn't exist. Live-validated against real Go, Rust, (the real `junit4` project's own `src/test/java`) Maven, (the real `sebastianbergmann/phpunit` project's own `tests/*Test.php`) PHP, and (the real `acejump/AceJump` project's own `src/test/kotlin`, with no `src/test/java` at all) Kotlin project layouts: all five correctly produce zero `test_gap` findings now

- [x] PEP 508 extras stripped from `version_constraint` — `analysis/dependencies.py::_PEP508_NAME_RE`/`_parse_pep508`. The regex matched only the bare package name (`^\s*([A-Za-z0-9][A-Za-z0-9._-]*)\s*(.*)$`), so a PEP 508 extras marker (`requests[security]==2.31.0`, `sqlalchemy[asyncio,mypy]>=2.0`) fell into the "rest" group and was stored verbatim as `version_constraint` (e.g. `"[security]==2.31.0"` instead of `"==2.31.0"`) — the shared parser both `_extract_pyproject_dependencies` (PEP 621 `[project.dependencies]`) and `_extract_requirements_txt_dependencies` use, so both manifest forms were affected. The parallel Poetry-table code path (`_poetry_version_constraint`) already correctly ignores an `extras` key; this PEP 508 string path was the one place extras weren't stripped. Concrete downstream effect, not just cosmetic: `analysis/update_intelligence.py::build_current_state`'s `_EXACT_PIN_RE` requires the constraint to start with `==`/a version digit, so a polluted `"[security]==2.31.0"` (starting with `[`) never matched — an extras-qualified exact pin was silently reported as `version_confidence=UNKNOWN` even though the manifest unambiguously pins one, disabling Update Intelligence/vulnerability-freshness checks for it; the raw polluted string was also persisted on `Dependency.version_constraint` and surfaced as-is in the Dashboard's dependency list. Fixed by widening the regex to optionally consume a bracketed extras group between the name and the constraint: `^\s*([A-Za-z0-9][A-Za-z0-9._-]*)\s*(?:\[[^\]]*\])?\s*(.*)$`. Live-validated end-to-end (`scan_structure` + `extract_dependencies_by_manifest`) against a real fixture with both `pyproject.toml` (`requests[security]==2.31.0`, `sqlalchemy[asyncio]>=2.0`) and `requirements.txt` (`uvicorn[standard]==0.30.0`): all three now correctly resolve to `name="requests"`/`version_constraint="==2.31.0"`, `name="sqlalchemy"`/`version_constraint=">=2.0"`, `name="uvicorn"`/`version_constraint="==0.30.0"` — no extras marker leaking into the constraint, where it previously would have

- [x] `check_dependency_updates` silently dropped same-name dependencies pinned at different current versions — `analysis/update_intelligence.py::check_dependency_updates`. Its `(ecosystem, name)` dedup (`seen: set[...]`) was meant to avoid repeating one network fetch for a package shared across components, but its `continue` skipped the *entire* per-dependency loop body, not just the fetch — so once one `Dependency` for `(ecosystem, name)` had been processed, every other `Dependency` sharing that same name (a real, already-documented scenario: `analysis/dependencies.py`'s own docstring says a monorepo with more than one manifest "can legitimately declare the same package name at different constraints, and those must not collide into one id") was dropped outright: no diff, no `ImpactAssessment`, no `UpdateLookupFailure` entry, no trace anywhere. Concretely, with `relationships` supplied (the real `si diagnose --check-updates` path, via `build_relationships`), the second component's own dependency simply never got update-checked, even though the data needed to assess it — a distinct `Dependency` with its own resolved version — was right there in the Snapshot; without `relationships` (the same-Snapshot dependency-scan fallback), the one surviving assessment's `affected_entity_ids` falsely included every component sharing that name, even one already on the latest version. Fixed by only deduplicating the network fetch itself (cached in a `fetched: dict[(ecosystem, name), AvailableState | None]`, since every real provider in `research/providers/*.py` resolves a package name to its latest version from the name alone — verified by reading each one), while still building a `current`/`diff`/`assess_impact` for every `Dependency`. Live-validated against the exact reported scenario (`left-pad` pinned at `1.0.0` in one component and already at the latest `1.3.0` in another, both against a provider reporting `1.3.0` available): the fetch is made exactly once, but both components now correctly get their own assessment (`review_required` for the `1.0.0` one, `no_update_available` for the `1.3.0` one), where the second was previously dropped entirely

## Epic 5 — Research
- [x] GitHub search adapter — `research/github.py`
- [x] source provenance — `Evidence` on every `ResearchResult`
- [x] candidate scoring — `research/scoring.py`; `CandidateAssessment.is_archived` is `bool | None` (`None` meaning "no archived signal available", e.g. `MCPRegistryResearchProvider`'s registry schema has no such field at all), but `rank_candidates`'s sort key and `cli/main.py`'s `research` command output both treated `None` identically to confirmed `False` (`0 if a.is_archived else 1`; `"archived" if assessment.is_archived else "not archived"`) — silently conflating "we don't know" with "verified not archived", the exact anti-hallucination guarantee `mcp_registry.py`'s own docstring promises ("must never synthesize a... signal to look consistent with `GitHubResearchProvider`") being broken one layer up in shared scoring/display code. Contrast with the same function's `is_recently_active`, which already correctly used a 3-way `active_rank = {True: 2, None: 1, False: 0}` so "unknown" is its own tier. `proposals/engine.py::_is_high_quality_candidate` had the same conflation (`not assessment.is_archived`), so an MCP-registry candidate with an unknown archived status could be silently promoted to "high quality" (and thus `adoption`, given license+activity) despite that status being unverified; `_insufficiency_reason` likewise never explained an unknown archived status, falling through to the generic "insufficient evidence" text. Fixed by giving unknown its own tier/label everywhere: `research/scoring.py`'s `not_archived_rank = {False: 2, None: 1, True: 0}` (mirroring `active_rank`), `cli/main.py`'s `_ARCHIVED_LABEL = {True: "archived", False: "not archived", None: "archived: unknown"}` (mirroring `_ACTIVITY_LABEL`), `_is_high_quality_candidate`'s `assessment.is_archived is False` (mirroring its own already-correct `is_recently_active is True`), and `_insufficiency_reason` adding a dedicated "archived status could not be determined" reason for `None`. Live-validated against the real MCP registry API (`si research filesystem --provider mcp-registry`): every result now correctly prints `archived: unknown` instead of the previously fabricated `not archived`
- [x] license extraction — `research/github.py`
- [x] maintenance signals — `research/github.py` (push recency, archived, stargazers — informational only, ADR-009)
- [x] research cache — `research/cache.py`; keyed purely by `(provider, query)`, never `limit` — but `si research QUERY --limit N` ("Maximum candidates to return.") only reaches `provider.search(query, limit=...)` on a cache miss (`cli/main.py`'s `research` command); both real providers genuinely honor `limit` there (`GitHubResearchProvider.search`'s `per_page=min(max(limit,1),30)` + `items[:limit]`, `MCPRegistryResearchProvider.search`'s analogous slicing), but a cache *hit* returned whatever was stored under the first `--limit` used for that query, regardless of what the current invocation asked for. Concretely: `si research flask` (default `--limit 10`) caches 10 results; `si research flask --limit 30` within the 6-hour TTL silently returned the same stale 10 with no indication the requested 30 weren't fetched, and the reverse (`--limit 30` then `--limit 3`) silently returned 30 where at most 3 were asked for — also affecting `--record`, persisting a wrong-sized batch of `ResearchResult`s to the evidence-trail JSON. Fixed by making `limit` part of the cache key (`_cache_key(provider, query, limit)`), so different `--limit` values for the same query simply never collide; `ResearchCache.get`/`.set` both gained a `limit: int = DEFAULT_LIMIT` parameter (defaulting to `si research`'s own default, so untouched call sites/tests keep working unchanged). Live-validated end-to-end via the real `si research` CLI command (`CliRunner`, stubbed HTTP layer): `--limit 10` then `--limit 30` for the same query now each correctly hit the network (2 calls, 2 separate cache files on disk), where previously the second call would have silently reused the first's 10-result cache entry; re-running `--limit 10` again correctly hit the cache

(A second provider, `research/mcp_registry.py` against the official MCP
Registry, was added beyond this epic's original PyPI/npm-agnostic scope.)

- [x] known-vulnerability lookup — `research/vulnerability_provider.py::VulnerabilityProvider` (a per-version "does this have any known advisories?" Protocol, distinct from `ComponentUpdateProvider`'s freshness question) implemented by `research/providers/osv.py::OSVVulnerabilityProvider` against OSV.dev, which covers pypi, npm, crates.io, Go, and Maven from one API. Wired into `analysis/update_intelligence.py::check_dependency_updates`/`ImpactAssessment` as informational `current_version_advisories`/`available_version_advisories` — never changes `UpdateVerdict` (a vulnerable current version does not by itself prove capability/dependency/interface impact was evaluated). `si check-updates --check-vulnerabilities` / `si dashboard --check-updates --check-vulnerabilities` (opt-in: one extra network request per resolved version). Live-validated against the real OSV.dev API (`lodash@4.17.20` correctly returns 5 real GHSA advisories; `org.apache.logging.log4j:log4j-core@2.14.1` correctly returns its real Log4Shell-era advisories under OSV.dev's `"Maven"` ecosystem name)
- [x] crates.io freshness provider — `research/providers/crates_io.py::CratesIoUpdateProvider` (`ComponentUpdateProvider` for the `cargo` ecosystem, same shape as `PyPIUpdateProvider`/`NpmUpdateProvider`). Response shape (`crate.max_stable_version`, the top-level `versions` list for release date/yanked status) verified live against the real API (`GET /api/v1/crates/serde`). Cargo's own bare-version convention (`serde = "1.0"` means `^1.0`, a caret range, unlike npm's bare-is-exact convention) is handled explicitly in `analysis/update_intelligence.py::build_current_state` (`_BARE_CONSTRAINT_IS_EXACT_PIN`) so a Cargo dependency is only ever treated as an exact current-version pin when explicitly written with `=` — never guessed from the bare form. `proposals/engine.py::change_plan_for_component_update` now also covers Cargo's *simple string form* (`name = "=1.0.219"`) via a raw-text regex substitution — the same discipline already used for pyproject.toml/package.json, not a full TOML parse/re-serialize (which would reformat the whole file). Cargo's *table* form (`name = { version = "=1.0.219", features = [...] }`) is deliberately never rewritten -- a nested `version` key isn't a name-adjacent literal a regex could safely, unambiguously locate -- so a table-form Cargo update still falls through to the external-implementer handoff, never a guessed patch
- [x] `ReleaseInfo.is_yanked` was faithfully captured by `research/providers/pypi.py`/`crates_io.py` from real registry data (PyPI's `info.yanked`, crates.io's per-version `yanked` field — confirmed live against a real yanked PyPI release, `cryptography==37.0.3`'s own `info.yanked: True`, `info.yanked_reason: "Regression in OpenSSL."`) but `analysis/update_intelligence.py` never read it anywhere — `_deprecation_diff_item` and `assess_impact`'s `NOT_ADVISABLE` branch both only ever checked the separate, top-level `ComponentState.is_deprecated` field. A yanked release (withdrawn by its publisher as broken/insecure — a stronger, more actionable signal than "deprecated") could therefore still silently reach `UpdateVerdict.UPDATE_RECOMMENDED`, actively recommending an upgrade to a version its own registry says not to use — a real, verified negative signal already collected being dropped one layer up, the same "producer populates it, no consumer reads it" shape as several earlier fixes this session, just on a Component Update Intelligence field rather than a Skill/Agent/Proposal one. Fixed by having both `_deprecation_diff_item` and `assess_impact` also check `release_info.is_yanked is True` (reusing `StateDiffCategory.DEPRECATED`/`UpdateVerdict.NOT_ADVISABLE` — a yanked release is a form of "the publisher says don't use this", not a distinct new category). Live-validated end-to-end (`diff_states`/`assess_impact`, the exact functions `si check-updates` calls) against the real yanked `cryptography==37.0.3` PyPI data above: now correctly produces a `deprecated`-category diff item and a `not_advisable` verdict with rationale "The latest available version has been yanked/withdrawn by its publisher.", where it previously would have proceeded to the ordinary version-delta verdict logic
- [x] `ReleaseInfo.released_at` — the same "producer populates it, no consumer reads it" shape as the `is_yanked` bullet immediately above, but with even stronger evidence: `analysis/update_intelligence.py`'s own module docstring names "release activity" (docs/05-analysis-engine.md's Lifecycle detector family) as exactly what this module implements, and `grep` confirms it is the only module in the codebase claiming that. All five ecosystem providers (`pypi.py`'s `upload_time_iso_8601`, `npm.py`'s `time[version]`, `crates_io.py`'s `created_at`, `go_proxy.py`'s `Time`, `maven_central.py`'s `lastUpdated`) faithfully populate `released_at` with real registry timestamps on every run, but nothing downstream — not `assess_impact`'s `verdict_rationale`, not `si check-updates`'s terminal output — ever read it, so a user had no way to see "how recently was this new version cut" despite the data being fetched every single time. Fixed by adding `_release_date_suffix` (`" (released YYYY-MM-DD)"` when known, `""` otherwise) into the `REVIEW_REQUIRED`/`UPDATE_RECOMMENDED` verdict rationale strings, and the equivalent inline into `cli/main.py`'s `check-updates` `available:` line (current-state never carries `release_info` at all — `build_current_state` only ever derives a version from the local manifest, so this is available-side only). Live-validated end-to-end against real PyPI freshness data (`si check-updates` against a real `requests==2.20.0` pin): now correctly prints `available: 2.34.2 (released 2026-05-14)` and the matching rationale text, where it previously showed only the bare version number
- [x] `ComponentState.changelog` — the same "producer populates it, no consumer reads it" shape as `is_yanked`/`released_at` above, on the same `analysis/update_intelligence.py`/`ComponentState` pipeline. `research/providers/pypi.py::_extract_changelog` builds real `ChangelogEntry(url=...)` records from a package's actual PyPI `project_urls` metadata whenever a `"changelog"`-labeled entry exists (a common, real-world case — e.g. `black`'s own `https://github.com/psf/black/blob/main/CHANGES.md`), and this flows all the way into `ImpactAssessment.state_diff.to_state.changelog`. But both consumers that turn an assessment into a user-facing artifact — `proposals/engine.py::propose_component_update` and `recommendations/engine.py::recommend_from_impact` — ignored it entirely, emitting only generic boilerplate ("review the changelog/release notes") even when the exact URL was sitting right there. Fixed by having both read `diff.to_state.changelog`: `propose_component_update` now cites the real URL(s) in its `implementation_stages` line (falling back to the generic text only when the list is empty — true for every ecosystem but pypi today, per each provider's own explicit "no structured changelog data" comment), and `recommend_from_impact` appends them to its `rationale`. Live-validated end-to-end against the real `black` PyPI package (`si check-updates black==22.0.0 --propose --record`): the recorded Proposal's `implementation_stages` now correctly reads `"Review the changelog before merging: https://github.com/psf/black/blob/main/CHANGES.md"`, and the printed Recommendation's rationale now includes the same URL, where both previously said only the generic "review the changelog" text
- [x] Go module proxy freshness provider — `research/providers/go_proxy.py::GoProxyUpdateProvider` (`ComponentUpdateProvider` for the `go` ecosystem, against `proxy.golang.org/<module>/@latest`, the same proxy `go get` itself uses). The proxy's module-path escaping rule (every uppercase letter becomes `!<lowercase letter>`) verified live against a real mixed-case module (`github.com/Masterminds/semver` → `github.com/!masterminds/semver`). Go's own `go.mod` convention is the opposite of Cargo's: every `require` line is already an exact, Minimal-Version-Selection-resolved version (or a pseudo-version), always `v`-prefixed and never a range, so `_EXACT_PIN_RE` gained an optional `v` prefix and `go` was added to `_BARE_CONSTRAINT_IS_EXACT_PIN` — without both, every Go dependency's current version would silently stay unresolved forever (the version string "v1.2.3" starts with "v", not a digit, so the pre-existing regex would never match it at all)
- [x] Maven Central freshness provider — `research/providers/maven_central.py::MavenCentralUpdateProvider` (`ComponentUpdateProvider` for the `maven` ecosystem, against `repo1.maven.org/maven2/<group-path>/<artifactId>/maven-metadata.xml` — the same metadata file Maven itself consults to resolve `RELEASE`/`LATEST` placeholders). Uses `<versioning><release>` (falling back to `<latest>` when `<release>` is absent) rather than the Maven Central Search API's own default sort order, because some coordinates publish more than one version *flavor* under the identical groupId:artifactId (Guava's `-jre`/`-android` builds, published independently and interleaved in time) — `<release>` is Maven Central's own authoritative pointer and resolves that ambiguity without this provider having to guess from a sorted list, verified live against `com.google.guava:guava`'s real metadata (`<release>33.7.1-jre</release>`). Maven's own pom.xml convention is like Go's, not Cargo's: a literal `<version>1.2.3</version>` (the only form `analysis/dependencies.py::_extract_pom_dependencies` ever extracts — a `${property}`-templated one is always skipped) means exactly that version, so `maven` was added to `_BARE_CONSTRAINT_IS_EXACT_PIN`; Maven's own range syntax (`[1.0,2.0)`, `(,2.0]`) starts with a bracket/paren character `_EXACT_PIN_RE` does not match, so it is correctly excluded with no Maven-specific range parser needed. End-to-end live-validated (`si check-updates --check-vulnerabilities` against a real pom.xml): `org.apache.logging.log4j:log4j-core@2.14.1` correctly resolves to available `3.0.0-beta3` with its real known advisories, and `com.google.guava:guava@30.0-jre` correctly resolves to available `33.7.1-jre`
- [x] `si dashboard --check-updates`/`si check-updates` own `--help`/docstring text still said "pypi/npm/cargo/go" in four places (`cli/main.py`'s `_DASHBOARD_CHECK_UPDATES_OPTION` help, `_CHECK_UPDATES_VULNERABILITIES_OPTION` help, and `check_updates()`'s own docstring, twice) even though `_update_providers()`/`_vulnerability_providers()` had already registered a `maven` entry each since the Maven Central provider above landed — confirmed via `git log` that PR only updated the `dashboard()` command's own docstring (which already correctly said "pypi/npm/cargo/go/maven") and missed these four sibling occurrences, the same "producer changed, consumer text didn't" drift pattern as the earlier `--plan-out` fix above, just a different, previously-unexploited set of lines in the same file. Directly user-facing and factually wrong: `--help` claimed "anything but pypi/npm/cargo/go today are skipped", while a real `pom.xml` maven dependency is in fact looked up. Fixed by appending `/maven` to all four. Live-validated: `si dashboard --help`/`si check-updates --help` now correctly print `pypi/npm/cargo/go/maven` everywhere

## Epic 6 — Reporting
- [x] static HTML generator — `reporting/html.py`; `_render_components`'s Detail-column dispatch enumerated `Skill`/`Document`/`Repository` but never `Agent` — a peer `Component` subtype with the same `is_standard_format` signal `Skill` already gets special-cased, per its own docstring ("mirrors `Skill.is_standard_format`"). The dashboard's JSON/JS path (`reporting/dashboard_data.py`/`dashboard_html.py`) already treats every `Component` kind uniformly, so this asymmetry was specific to the static single-file report: every discovered Agent rendered with a permanently-empty Detail column. Fixed by adding an `Agent` branch (same "standard format"/"non-standard format" wording as `Skill`, plus `model_provider` when set). Live-validated (`si report`) against a real fixture: a `.claude/agents/code-reviewer.md` Agent now correctly renders `standard format (model: sonnet)`
- [x] graph visualization — `reporting/dashboard_html.py`'s capability provider SVG graph subtab. `generate_dashboard_html`'s outer HTML shell interpolated `overview.target_name` (a local directory's basename, or the raw GitHub locator string on the CLI — `discovery/target.py`, not sanitized by anything upstream) directly into the `<title>` and sidebar `<p>` lines with no HTML escaping, unlike its sibling `reporting/html.py`'s static report, which already imports `escape` from `html` and applies it to the identical title-line pattern. Live-reproduced: a target named `<img src=x onerror=alert(1)>` broke out of both tags and injected a live `onerror` handler that fired the moment the generated `dashboard.html` (explicitly designed to be opened via `file://`, per this module's own docstring) was opened in a browser — a genuine stored-XSS, not a hypothetical one, since the client-side JS's own `innerHTML`-free discipline the module docstring describes never applied to this server-side-rendered outer shell. Fixed by importing `escape` and applying it to `target_name` everywhere it's interpolated into the shell, mirroring `reporting/html.py`'s own pattern exactly. Live-validated: the same malicious target name now renders as the inert, correctly-escaped `&lt;img src=x onerror=alert(1)&gt;` in both spots, while the JSON payload (already safely sandboxed inside a `&lt;/`-escaped `&lt;script type="application/json"&gt;` block, per the pre-existing `test_generate_dashboard_html_escapes_closing_script_tag_in_data`) still correctly round-trips the real, unescaped string
- [x] `reporting/html.py::_render_dependency_graph` misattributed every dependency to the Repository — it flattened every dependency across all components into one list and drew every edge from a single, hard-coded node permanently labeled `"repository"`, never consulting which Component actually owns each `Dependency`. But `analysis/dependencies.py::attach_dependencies_by_component` already correctly attributes each `Dependency` to its real owning Component (e.g. a Skill's own `package.json`, not the Repository's) — exactly the fact `_render_capability_graph` two functions above already resolves correctly via `component_names = {c.id: c.name for c in snapshot.components}` for capability `provider_ids`, making the dependency graph's hard-coded label look like a plain oversight rather than a deliberate simplification. `si report`'s static HTML report is this project's flagship human-readable deliverable — a reviewer auditing "does this repo pull in this package directly, or does it come in via a Skill" got a visually wrong answer every time: every dependency looked like a direct repository dependency. Fixed by mirroring `_render_capability_graph`'s own pattern: each row now labels its source with the real owning component's name (resolved via the same `component_names` map), falling back to the component's own id only if somehow unresolvable. Live-validated against a real fixture (a root `package.json` declaring `top-level-dep` and a Skill `demo` with its own `package.json` declaring `left-pad`): `left-pad`'s row now correctly labels its source `demo`, where it previously always rendered `repository`
- [x] Dashboard Overview's "Changes" card ignored `--check-updates`-driven changes — `reporting/dashboard_html.py::renderOverview`. `DATA.changes` is populated from two independent sources in `dashboard_data.py::build_dashboard_data` (a `--compare-with` snapshot diff, and/or a `--check-updates` run's `_update_availability_changes`, which always emits at least one `StateDiffCategory.CHANGED` item whenever a dependency's current and available versions differ), but the Overview card branched only on `has_previous_snapshot` — so running the single most natural `si dashboard <target> --check-updates` invocation (no `--compare-with`) unconditionally showed "No previous snapshot to compare... Run si dashboard --compare-with ..." even when `DATA.changes` held real, non-empty update-availability changes, one click away on the separate Changes tab. This directly contradicted `renderChanges()` (same file), which already correctly checks both `has_previous_snapshot` and `has_update_check` and gives the right reason for each — the Overview card was the same data, wrong condition. Fixed by branching on `DATA.changes.length` itself and mirroring `renderChanges()`'s own two-reason messaging when it's empty; also renamed the section heading from "Changes since previous snapshot" to the generic "Changes" (matching the Changes tab's own label), since "since previous snapshot" is no longer universally true. Live-validated in a real headless Chromium session (Playwright): a fixture with one `ImpactAssessment` and no previous snapshot (`--check-updates` only) now correctly renders "1 change(s) detected." in the Overview, where it previously always rendered the misleading "No previous snapshot to compare" message
- [x] Proposals detail view — `reporting/dashboard_html.py::toggleProposalDetail`; click-to-expand a Proposal row (same interaction pattern as `toggleComponentDetail`) to show `capabilities`/`interfaces`/`test_strategy`/`documentation_requirements`/`rollback_strategy`/etc. — all of these already existed on `Proposal` but the Proposals table previously rendered only `kind`/`problem`/permission/insufficiency-reason, with no way to see the rest without opening the raw JSON snapshot. Interactively verified with a real browser (Playwright/Chromium): expand/collapse both work, no console errors, populated `interfaces` (from the target-kind feature) renders correctly
- [x] dashboard JSON export — `reporting/dashboard_data.py::DashboardData`/`build_dashboard_data`; also the documented external consumer boundary (`docs/design/docs/12-storage-and-state.md`). `_collect_evidence` (backing the Dashboard's dedicated, filterable "Evidence" tab) only ever walked components/dependencies/capabilities/findings/research/update-assessments — `snapshot.adrs`/`relationships`/`recommendations`/`proposals`/`verification` are all first-class `Snapshot` lists whose entities (`ADR` via the `Entity` base class; `Relationship`/`Recommendation`/`Proposal`/`Verification` each declaring their own `evidence: list[Evidence]`) are genuinely populated with real, freshly-created `Evidence` by their own producers (`discovery/adr.py`, `verification/engine.py`, `proposals/engine.py`, `recommendations/engine.py`) — none of it ever reached the Evidence tab at all, not merely mislabeled. `ADR`/`Verification` evidence in particular is unique `Evidence` that appears nowhere else in the snapshot, so it was completely invisible to a user inspecting "what proves this was detected/this test ran" via that tab. Fixed by adding the five missing loops, labeled the same way as the existing ones (`adr:{name}`, `relationship:{type}`, `recommendation:{id}`/`proposal:{id}`/`verification:{id}` — an honest `.id` where no other short, non-fabricated label exists, matching the existing convention of never inventing one). Live-validated against a real fixture (`docs/adr/ADR-001-*.md` with a `Status:` line): both of that ADR's own `Evidence` records (the file match and the parsed status line) now correctly appear in `DashboardData.evidence`, where they were previously silently dropped
- [x] snapshot diff — `reporting/diff.py`
- [x] Component detail's "Update assessments" panel always showed "Not checked." — `reporting/dashboard_html.py::toggleComponentDetail`. Its filter compared a Component's own id (`c.id`, e.g. `"repository:root"`) against `a.state_diff.identity.component_id` — but that field is always a *Dependency* id (`analysis/update_intelligence.py::_identity_for` sets `component_id=dependency.id`, load-bearing for the `DEPENDS_ON` relationship graph walk; `core/ids.py::stable_id` prefixes the two id spaces differently, `"dependency:pypi:..."` vs `"repository:..."`, so they can never collide) — never a Component id. The correctly-computed list of real Component ids an assessment affects is `ImpactAssessment.affected_entity_ids`, already used correctly two sections earlier in this same file's `renderUpdates`'s own "Affects: " line, and for `findings` two lines above this one. So clicking any Components-table row to expand its detail panel always rendered "Not checked." under "Update assessments", even when `si dashboard --check-updates` found a real, high-confidence assessment concretely affecting that exact component's dependencies (correctly shown, separately, in the "Updates" tab) — silently dead code since the dashboard's original implementation (PR #2), unchanged through every later Update Intelligence PR. Fixed by filtering on `affected_entity_ids` instead, mirroring the adjacent `findings` filter. Live-validated in a real headless Chromium session (Playwright): a fixture with one component and one matching `ImpactAssessment` (`affected_entity_ids=["r1"]`) now correctly renders `review_required — left-pad has an update available (1.0.0 -> 1.3.0)` in that component's expanded detail row, where it previously always rendered "Not checked."
- [x] `renderUpdates`'s "Affects: " line leaked raw Capability ids instead of names — `reporting/dashboard_html.py`. Its `a.affected_entity_ids.map(componentName)` assumed every id in `ImpactAssessment.affected_entity_ids` names a Component, but `analysis/update_intelligence.py::_traverse_affected` (the relationship-graph walk backing it) explicitly continues onward across `PROVIDES`/`USES` edges from a changed Component into the Capabilities it provides and their consumers — documented on both `_traverse_affected` itself and `core/impact.py`'s own field description ("Component ids... plus any Capability and consumer-Component ids"). `componentName` only ever searched `DATA.components`, so a Capability id fell through to its own `id ? ... : id` fallback and rendered the raw internal id string (e.g. `capability-3f9a2b1c8d4e`) as a chip instead of that capability's real, human-readable name — reachable in ordinary use any time an updated dependency is consumed indirectly via a Capability (Component --provides--> Capability --used by--> Component), a common shape. `Finding.affected_entity_ids`'s own `componentName` call sites were unaffected (verified: every producer in `analysis/*.py` only ever attaches Component/Repository ids there), so only this one call site needed fixing. Fixed by adding an `entityName` helper that falls back to `DATA.capabilities` when `componentName` can't resolve the id, used only at this one call site. Live-validated in a real headless Chromium session (Playwright): a fixture with one component, one capability, and one `ImpactAssessment` whose `affected_entity_ids` includes both ids now correctly renders `Affects: demo-repo, padding-util`, where the capability previously rendered as its raw id (`cap-1`)
- [x] `Verification.regressions_found` never surfaced in either report, so a passing-but-regressed run could look like plain success — `reporting/html.py::_render_verification` and `reporting/dashboard_html.py`'s component-detail "Verifications" block. `si verify` (`cli/main.py`) can compute `regressions_found` independently of the command's own exit code (via `--before-snapshot`/`--after-snapshot`'s diff, R10's before/after comparison) and explicitly treats a passing command with non-empty `regressions_found` as a failing run too (`if not verification.tests_passed or verification.regressions_found: raise typer.Exit(code=1)`) — but both report surfaces derived their status purely from `tests_passed`: the static report only ever printed `passed: {status}`, and the dashboard's badge (`badge(label, v.tests_passed ? "success" : "critical")`) could read green "success" for a `Verification` the CLI itself flagged and exited non-zero for. Not merely unpopulated upstream: `reporting/dashboard_data.py` already embeds the full `Verification` model (including `regressions_found`) into `DATA.verifications`'s JSON payload — the dashboard JS had the field available and simply never read it. Fixed by appending each `regressions_found` entry to the static report's `<li>` (still shown even when `tests_passed` is `True`), and by deriving the dashboard badge from `tests_passed && !regressions_found.length` plus listing the regression strings underneath, the same nested-list pattern already used for findings/update assessments in that same block. Live-validated in a real headless Chromium session (Playwright) and against the static report: a `Verification(tests_passed=True, regressions_found=["a new HIGH-severity finding appeared after the change"])` fixture now correctly renders `passed: True — regressions found (1): a new HIGH-severity finding appeared after the change` in the static report, and a `badge-critical` (not `badge-success`) with the regression text listed underneath in the dashboard's component detail panel — where both previously showed plain, misleading success

## Epic 7 — Improvement
- [x] recommendation engine — `recommendations/engine.py`; `_EFFORT_BY_CATEGORY` (a plain `dict[str, str]`, since `Finding.category` is a plain `str`, not an enum, so nothing type-checks it against every category a producer can emit) was missing the two categories `analysis/gaps.py::audit_capability_gaps` produces (`capability_gap`, `invalid_requirements_file`) — both fell through to `"unknown"` effort, which in turn forced an artificially `"low"` risk regardless of the Finding's own `Severity.HIGH`. Fixed by adding both (`capability_gap` -> `"medium"`, `invalid_requirements_file` -> `"small"`, matching the effort semantics already used for similarly-scoped categories). Live-validated (`si improve`) against a real declared-but-missing capability: the recommendation now correctly shows `effort: medium, risk: medium` instead of `effort: unknown, risk: low`
- [x] `recommendations/engine.py::recommend_from_impact`/`generate_update_recommendations` (Component Update Intelligence's own "... -> Impact -> Recommendation" step, per that module's docstring) were fully implemented and unit-tested but never called anywhere outside their own tests — no CLI command ever turned an `ImpactAssessment` into a `Recommendation`. `si check-updates` only ever printed the raw assessment and, with `--propose`, jumped straight to a `Proposal` (skipping the Recommendation step entirely); `si dashboard --check-updates` showed the raw assessments in their own section but the Recommendations screen only ever reflected Finding-based recommendations (`generate_recommendations`), never an update verdict; `si improve` — the command literally named for a ranked recommendation plan — never ran Update Intelligence at all. Fixed by calling `generate_update_recommendations` in `check_updates()` (prints each one, and with `--record` appends to `recommendations.json`, the same accumulator file `si dashboard --compare-with` already reads) and by merging its output into `snapshot.recommendations` in `dashboard()` before `build_dashboard_data` when `--check-updates` is set, so an actionable update verdict now reaches the same Recommendations screen as Finding-based ones instead of only its own separate update-check section. Live-validated end-to-end against real PyPI (`requests==2.20.0` pinned in a real pyproject.toml): `si check-updates` now prints `Update requests from 2.20.0 to 2.34.2.` as a recommendation, and `si dashboard --check-updates`'s `recommendations` JSON array now includes that same objective alongside the Finding-based ones
- [x] proposal schema — `core/proposals.py`
- [x] new Skill proposal / new Agent proposal / new software proposal — `proposals/engine.py::propose_solution`'s optional `target_kind: ComponentKind` (`si propose --target-kind skill|agent|mcp_server|...`) shapes `test_strategy`/`documentation_requirements` to how that kind is actually verified in practice (a Skill's contract is its own SKILL.md, an Agent is verified by running scenarios, an MCP server by its tool/resource schema — see `_TEST_STRATEGY_BY_KIND`), and populates `Proposal.interfaces` with that kind's own explicit, already-detected discovery convention where one exists (`_INTERFACES_BY_KIND`: a Skill's SKILL.md front matter, an Agent's `.claude/agents/*.md` front matter, an MCP server's tool/resource schema, ...) — `interfaces` was previously never populated by any caller at all (confirmed: no code anywhere set it), so this closes a real, concrete gap rather than a "someday" one. `si propose`'s terminal output was also fixed to print `capabilities`/`interfaces`/`test_strategy`/`documentation_requirements`/`rollback_strategy` when non-empty — all five existed on `Proposal` already but none were ever echoed to the terminal, only visible via `--out`'s JSON. `_DOCUMENTATION_REQUIREMENTS_BY_KIND` was missing `TOOL`/`DOCUMENT` entries its two sibling tables (`_TEST_STRATEGY_BY_KIND`, `_INTERFACES_BY_KIND`) already had dedicated wording for — unlike `_INTERFACES_BY_KIND`'s own doc comment, which explicitly justifies its DOCUMENT exclusion, this table's omission had no such justification and looked like a plain oversight from when it was authored alongside `_TEST_STRATEGY_BY_KIND`. Fixed by adding both. Live-validated (`si propose --target-kind tool`/`--target-kind document`): both now correctly print kind-specific documentation-requirements wording instead of silently falling back to the generic text. The full field-level *schema* per kind this bullet previously deferred ("`interfaces` stay `list[str]` guidance, not a structured per-kind object") is now closed too: added `core/proposals.py::InterfaceField` (`name`/`required`/`description`) and `Proposal.interface_fields: list[InterfaceField]`, populated by a new `_INTERFACE_FIELDS_BY_KIND` table for exactly the two kinds `_INTERFACES_BY_KIND` already names a single, verified convention for — SKILL (`name`, `description` required; `allowed-tools`, `disallowed-tools`, `paths` optional) and AGENT (`name`, `description` required; `model`, `tools`, `disallowedTools` optional) — each field name/attribute mapping read directly out of `discovery/skills.py`/`discovery/agents.py`'s own real frontmatter parsing (never an invented schema, matching `_INTERFACES_BY_KIND`'s own evidence-first discipline; `capabilities` was deliberately left as free-text, since it holds the caller's own stated requirements, not a fixed per-kind contract). Wired into all three `propose_solution` `Proposal(...)` construction sites (creation/adoption/integration), `si propose`'s terminal output (`Interface fields:` with a `required`/`optional` marker per field), and the dashboard's Proposal detail toggle. Live-validated: `si propose --target-kind skill`/`--target-kind agent` now correctly print all 5 fields for each kind with accurate required/optional markers; a real headless Chromium session (Playwright) confirms the dashboard's Proposal detail panel renders the same field list
- [x] `Proposal.security_considerations` — docs/07-improvement-engine.md's "Proposal must contain" list requires "security implications" alongside problem/evidence/interfaces/capabilities/dependencies/implementation stages/tests/documentation/rollback strategy, and the field has existed on `core/proposals.py::Proposal` since it was defined, but nothing anywhere ever set it — every one of `propose_solution`'s three `Proposal(...)` construction sites (creation/adoption/integration) and `propose_component_update`'s left it `None`, so every real Proposal's dashboard rendering (`reporting/dashboard_html.py`'s `p.security_considerations || "None recorded."`) silently showed "None recorded." for a documented required field, and `si propose`'s terminal output (which already prints `capabilities`/`interfaces`/`test_strategy`/`documentation_requirements`/`rollback_strategy` when non-empty, see the bullet below) never had a line for it either, the same drift. Fixed by adding `_SECURITY_CONSIDERATIONS`/`_UPDATE_SECURITY_CONSIDERATIONS` (mirroring `_TEST_STRATEGY`/`_UPDATE_TEST_STRATEGY`'s existing generic-fallback pattern exactly) plus a per-`ComponentKind` `_SECURITY_CONSIDERATIONS_BY_KIND` table matching `_TEST_STRATEGY_BY_KIND`'s own shape and kind coverage (SKILL/AGENT/MCP_SERVER/TOOL/WORKFLOW/DOCUMENT/REPOSITORY), wired into all four `Proposal(...)` calls and into `si propose`'s print block. Live-validated (`si propose --target-kind skill`): now correctly prints `Security considerations: Review the Skill's own allowed-tools/disallowed-tools frontmatter for scope creep beyond what it actually needs.` instead of nothing, and `--record`'s `proposals.json` now carries the same text where it previously carried `null`
- [x] `Proposal.implementation_stages` for creation/adoption/integration Proposals — the same "documented required field, never populated" shape as the `security_considerations` bullet immediately above, on the very same field list ("implementation stages" is one of the sibling entries in docs/07-improvement-engine.md's "Proposal must contain"). `propose_component_update` (the `component_update`-kind construction site) already set three concrete steps, but `propose_solution`'s three `Proposal(...)` sites — which produce every `creation`/`adoption`/`integration` Proposal, i.e. the entire main improvement-engine decision tree, not just the update-intelligence side path — left it at its `[]` default, so `reporting/dashboard_html.py`'s "Implementation stages" section (`listOrNone(p.implementation_stages)`) showed "None recorded." for the overwhelming majority of real Proposals, and `si propose`'s terminal output had no line for it at all (the same compounding omission `security_considerations` had). Fixed with the identical pattern: `_IMPLEMENTATION_STAGES`/`_IMPLEMENTATION_STAGES_BY_KIND` (SKILL/AGENT/MCP_SERVER/TOOL/WORKFLOW/DOCUMENT/REPOSITORY, matching `_TEST_STRATEGY_BY_KIND`'s shape), wired into all three `propose_solution` `Proposal(...)` calls and into `si propose`'s print block (next to `interfaces`). Live-validated (`si propose --target-kind skill`): now correctly prints `Implementation stages: Author the SKILL.md front matter and instructions., Add any scripts/references/assets it bundles., Verify it through its own declared entry points per the test strategy.` instead of nothing
- [x] implementation plan generator — `execution/plan.py::ChangePlan` (local branch/commit plan; the remote Draft PR step is `execution/github_pr.py`, see Epic 8)
- [x] Proposal -> ChangePlan (deterministic case only) — `proposals/engine.py::change_plan_for_component_update`; a `pypi`, `npm`, `go`, or `cargo` (simple-string-form only) dependency whose current constraint is confirmed as an exact pin (`analysis/update_intelligence.py::_EXACT_PIN_RE`, which also recognizes npm's and Go's own bare-version conventions while explicitly rejecting npm's `x`/`X`/`*` wildcard segments) can be turned into an executable `ChangePlan` with no human/LLM authoring a diff by hand (`si check-updates --plan-out DIR` -> `si execute <file> <target> --approve`). The `go.mod` patcher (`_patch_go_mod_pin`) rewrites a literal `require module version` entry in place — either the single-line form or a line inside a `require (...)` block — matching only the exact text the assessment observed and preserving indentation and any trailing `// indirect` comment verbatim; the Cargo.toml patcher (`_patch_cargo_toml_pin`) covers only the simple string form, never the table form; `maven` still falls through to the external-implementer handoff (pom.xml's XML-escaping rules are more than a plain regex substitution should attempt). `_patch_pyproject_pin` now also matches Poetry's own native `[tool.poetry.dependencies]` table's simple string form (`requests = "2.31.0"`, no `==` operator, a bare unquoted TOML key) alongside the PEP 621 array form it already covered — `analysis/dependencies.py` had already recognized this shape as an exact pin (see the dependency-graph entry above), but the patcher's regex only matched a fully-quoted `"name==version"` token, so `change_plan_for_component_update` silently returned `None` for every Poetry-native project even at `UPDATE_RECOMMENDED`/`HIGH` confidence — the table-of-dict form (`requests = { version = "..." }`) still correctly returns `None`, same discipline as Cargo.toml's own table form. `pypi` now dispatches to a third patcher, `_patch_requirements_txt_pin` (via `_select_patcher`, keyed on the manifest's own filename rather than ecosystem string alone), for requirements.txt's bare, unquoted `name==version` lines — arguably the single most common pypi pinning convention (`pip freeze > requirements.txt`), and previously never matched by any patcher at all since `_patch_pyproject_pin` was unconditionally selected for every `pypi` dependency regardless of which manifest it actually came from; a trailing `;`-environment-marker or `#`-comment is preserved verbatim, same as go.mod's own trailing-comment handling. Live-validated end-to-end (`si check-updates --plan-out`) against a real multi-package requirements.txt with live PyPI freshness data: each of three real, independently-pinned packages (click/requests/flask) correctly produces its own ChangePlan patching only its own line, preserving the rest of the file verbatim — and, separately, a real Poetry-native pyproject.toml's bare-pinned `requests = "2.31.0"` still correctly produces an executable ChangePlan patching only that line to the real latest version, preserving the rest of the file verbatim. `cli/main.py`'s own `--plan-out` help/docstring/zero-plans-written text still said "pypi or npm ... only" (stale since go/cargo support landed in this same session, confirmed via `git log` that those two commits updated README.md but never `cli/main.py`) — corrected to name all four, with the Cargo table-form/other-ecosystem exclusions spelled out; `README.md` already had this right
- [x] `si check-updates --plan-out` used the wrong repository root for a GitHub target — `cli/main.py`'s `check_updates` command. `discover_local_repository(target)` resolves a GitHub spec (`owner/repo` or a `https://github.com/...` URL, explicitly supported by the shared `_TARGET_ARGUMENT`) by shallow-cloning it into a fresh temp directory and recording that clone path as `snapshot.target.locator` — every other consumer that needs "the actual repository root" reads it from there (e.g. `analysis/engine.py`'s `root = Path(snapshot.target.locator)`). But `--plan-out`'s call to `change_plan_for_component_update(assessment, Path(target))` passed the raw, unresolved CLI argument string instead — for a local path this happens to equal the resolved root (why it went unnoticed), but for a GitHub target `Path("octocat/demo-repo")` is not a real directory, so `change_plan_for_component_update`'s manifest read fails and it returns `None` for every assessment, indistinguishable from the legitimate "not deterministic" case (a range constraint, an unsupported ecosystem) with the exact same generic "No ChangePlan could be generated deterministically" message — silently producing zero plans for every GitHub target, defeating `--plan-out`'s whole purpose of feeding a fully automated `si execute --approve` loop. Fixed by using `discovery.snapshot.target.locator` (already in scope) instead of the raw `target` string. Live-validated (`si check-updates octocat/demo-repo --plan-out`) against a real mocked GitHub clone (matching this test suite's own `clone_github_repository` mocking pattern) with a real `pyproject.toml` pinning `pydantic==2.0.0` against a fake PyPI response reporting `2.9.0`: a real `ChangePlan` (`branch_name="si/update-pydantic-to-2.9.0"`) is now correctly written, where it previously always silently produced zero plan files
- [x] Proposal -> external-implementer handoff (every other case) — `execution/handoff.py::build_handoff_packet` (`si propose --target <path> --handoff-out <file>`); a self-contained JSON packet (Proposal + target + the exact `ChangePlan` file schema an implementer must hand back) for creation/adoption/integration Proposals and any range-constrained update, which still have no automatic path to a ChangePlan and never will inside this module — deciding the actual diff remains a job for a human or an external implementer (e.g. Claude Code), never guessed here (ADR-007). See `docs/design/docs/21-external-implementer-handoff.md`

## Epic 8 — Governance
- [x] policy schema — `core/enums.py` (`PermissionLevel`, `FORBIDDEN_BY_DEFAULT_ACTIONS`), `policy/engine.py`
- [x] approval records — `core/governance.py::Approval`
- [x] audit log — `policy/engine.py::audit_log_entry` builds an `AuditLogEntry` per policy decision; `Snapshot.audit_log`/`audit_log.json`; wired into `si execute --record`
- [x] GitHub write scopes — `execution/github_pr.py::push_branch`/`open_draft_pull_request`, gated by `policy.evaluate` under `create_draft_pr` (a distinct action/Approval from the local `create_local_branch_and_commit` step); token via the `GITHUB_TOKEN` environment variable, never a CLI argument
- [x] Draft PR adapter — `execution/github_pr.py::open_draft_pr_for_plan`; pushes the branch `local_git.apply_plan` already created and opens it as a Draft PR via the GitHub REST API. `si execute --push --repo owner/repo`. Live-validated: `push_branch` against a real throwaway branch on this repo (pushed, verified via the GitHub API, branch left for manual cleanup since even a scratch `git push --delete` was blocked by this session's own safety classifier); the REST "open PR" call itself could not be validated live from this sandbox (raw HTTP to `api.github.com` is blocked here), so it's covered by mocked-HTTP unit tests only, matching every other GitHub-API-touching adapter in this codebase

## Epic 9 — Verification
- [x] test runner adapter — `verification/engine.py::run_verification`
- [x] post-change scan — `si diagnose --out` before and after a change, feeding the next two items
- [x] before/after comparison — `si diff <before> <after>` (`reporting/diff.py`)
- [x] regression detection — `si verify --before-snapshot <dir> --after-snapshot <dir>` populates `Verification.regressions_found` from that diff's added findings
- [x] drift detection over time — `si watch <target>` (`docs/design/docs/16-roadmap.md` Phase 8's "scheduled scans"/"drift detection"/"regression alerts", scoped to a single target — no ecosystem-wide history, see Epic 10's still-unchecked "multi-repo manifest"). Deliberately not a daemon (ADR-004): each run is `si diagnose` plus `si diff`'s own engine (`reporting/diff.py::diff_snapshots`) against a self-managed `--state-dir` history — one immutable snapshot subdirectory per run (never overwritten, per `Snapshot.write_to_directory`'s own convention) plus a `latest.txt` pointer. "On an interval" means invoking `si watch` again later by hand, cron, or a CI schedule — this project has no polling loop or background-process machinery anywhere, and `watch` does not introduce any

## Epic 10 — Reference ecosystem
- [ ] AI Video Production OS fixture — used only as an external, read-only validation target (cloned locally, never committed as a fixture in this repo)
- [ ] multi-repo manifest — one Snapshot is one target repository scan; SI does not aggregate multiple Snapshots into an ecosystem-wide manifest (see `docs/design/docs/12-storage-and-state.md`, "What this boundary does not cover")
- [ ] dashboard integration — the *SI* Dashboard is done (Epic 6); an external ecosystem-level dashboard consuming SI's boundary is a separate, other-repository concern
- [ ] public demo
