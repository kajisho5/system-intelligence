"""Deterministic and semantic analyzers over discovered evidence.

Not yet implemented. Planned scope: dependency graph construction,
architecture heuristics, capability extraction, duplication detection,
capability gap detection, and potentially-unused classification (never
`verified_unused` from static non-reference alone — see ADR-010). See
docs/design/docs/05-analysis-engine.md.
"""
